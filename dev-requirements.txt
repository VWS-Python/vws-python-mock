PyYAML==5.3.1
Sphinx-Substitution-Extensions==2020.5.30.0
Sphinx==3.0.4
VWS-Auth-Tools==2020.5.31.0
VWS-Test-Fixtures==2019.12.13.1
attrs==19.3.0  # Modern attrs is required for pytest
autoflake==1.3.1
check-manifest==0.42
codecov==2.1.4 # Upload coverage data
doc8==0.8.1
dodgy==0.2.1  # Look for uploaded secrets
flake8-commas==2.0.0  # Require silicon valley commas
flake8-quotes==3.2.0  # Require single quotes
flake8==3.8.2  # Lint
freezegun==0.3.15  # Freeze time in tests
isort==4.3.21  # Lint imports
keyring==21.2.1
mypy==0.780 # Type checking
pip-check-reqs-pip-gte-20==2.0.3.4
pydocstyle==5.0.2  # Lint docstrings
pyenchant==3.1.1  # Bindings for a spellchecking sytem
pygithub==1.51
pylint==2.5.3  # Lint
pyroma==2.6  # Packaging best practices checker
pytest-cov==2.9.0  # Measure code coverage
pytest-envfiles==0.1.0  # Use files for environment variables for tests
pytest==5.4.3  # Test runners
sphinx-autodoc-typehints==1.10.3
sphinx_paramlinks==0.4.2
sphinxcontrib-spelling==5.1.0
timeout-decorator==0.4.1  # Decorate functions to time out.
twine==3.1.1
vulture==1.5
yapf==0.30.0  # Automatic formatting for Python

# Ideally we would use flaky or pytest-rerunfailures.
# However, we sometimes hit ``UnexpectedEmptyInternalServerError`` in a fixture
# and we want to retry in that case.
# ``flaky`` does not allow us to retry when an error happens in test setup:
#  https://github.com/box/flaky/issues/135.
#
# ``pytest-rerunfailures`` does not allow us to specify which exceptions to
# retry on:
#  https://github.com/pytest-dev/pytest-rerunfailures/issues/58.
#  https://github.com/pytest-dev/pytest-rerunfailures/issues/101.
flaky-with-setup-checks==3.5.3
